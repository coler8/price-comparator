import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pricecomparator.app',
  appName: 'price-comparator',
  webDir: 'www'
};

export default config;
